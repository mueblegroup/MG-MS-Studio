<?php
namespace App\Http\Middleware;
use App\Models\Studio;use App\Models\StudioDomain;use App\Support\TenantManager;use Closure;use Illuminate\Http\Request;use Illuminate\Support\Facades\Schema;use Symfony\Component\HttpFoundation\Response;
class ResolveStudioTenant{public function handle(Request $r,Closure $n):Response{$s=$this->resolve($r);app(TenantManager::class)->set($s);if($s){app()->instance('studio',$s);app()->instance('studio.id',$s->id);}return $n($r);}private function resolve(Request $r):?Studio{if(!Schema::hasTable('studios'))return null;$h=strtolower($r->getHost());if(Schema::hasTable('studio_domains')){$d=StudioDomain::where